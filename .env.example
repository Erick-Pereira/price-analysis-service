# Copie para .env ao correr Simcag.PriceAnalysisService.Api (DotNetEnv).

# PostgreSQL (padrão)
ConnectionStrings__DefaultConnection=Host=localhost;Port=5432;Database=simcag_pricing;Username=postgres;Password=postgres
# Aliases legados (vão ser removidos): DB__*

# HTTP → Market Data Service (default no código: http://localhost:5007)
# MARKET_DATA_API_URL=http://localhost:5007
# MARKETDATA_SERVICE_URL=http://localhost:5007
# MarketData__BaseUrl=http://localhost:5007

# Headers internos para downstreams com Gateway auth (price-analysis → market-data)
# SIMCAG_INTERNAL_SERVICE_USER_ID=service:price-analysis
# SIMCAG_GATEWAY_DOWNSTREAM_HMAC_SECRET= (opcional; se definido, inclui prova HMAC)

# Redis (opcional) — cache de referência de mercado; se omitido, usa NullMarketDataCache
# REDIS__CONNECTION=localhost:6379,abortConnect=false

# RabbitMQ
RABBITMQ__HOST=localhost
RABBITMQ__PORT=5672
RABBITMQ__USERNAME=guest
RABBITMQ__PASSWORD=guest
RABBITMQ__VIRTUALHOST=/
# RABBITMQ__EVENTS_EXCHANGE=events
