name: price-analysis-service

on:
  push:
    branches: [main, master]
    paths:
      - "price-analysis-service/**"
      - "shared/**"
      - ".github/workflows/price-analysis-service.yml"
  pull_request:
    paths:
      - "price-analysis-service/**"
      - "shared/**"
      - ".github/workflows/price-analysis-service.yml"
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"
      - name: Test
        run: dotnet test price-analysis-service/Simcag.PriceAnalysisService.Tests/Simcag.PriceAnalysisService.Tests.csproj -c Release

  build-and-push:
    needs: test
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          push: true
          context: .
          file: price-analysis-service/Simcag.PriceAnalysisService.Api/Dockerfile
          tags: shelow/price-analysis-service:latest
